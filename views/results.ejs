<%- include('header') %>


   <!-- GRID -->
   <div class="container">
      <div class="row">
          <div class="col-12 offset-lg-6 col-lg-6">
            <!--liste les résultats section-->
            
            <ul class="list-group">

              <% for(var i=0;i<search.length;i++){%>

              <li class="list-group-item">
                <span><%= search[i].departure %></span>
                <i class="fas fa-long-arrow-alt-right"></i>
                <span><%= search[i].arrival %></span>
                <span>Train departure at: <%= search[i].departureTime %></span>
                <span><%= search[i].price %>€</span>
                <a class="stretched-link" href="/orders?id=<%= search[i]._id %>">Go</button>
              </li>
              <%}%>

            </ul>
          </div>
        </div>
      </div>
   </div>
   
<%- include('footer') %>